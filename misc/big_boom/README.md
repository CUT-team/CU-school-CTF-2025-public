# big_boom | medium | misc

## Информация

> Т-т-т-т-ёмный ло-о-о-о-орд, это всё, что мы нашли...
>
> nc <ip> 31337

## Деплой

```sh
cd deploy
docker-compose up --build -d
```

## Выдать участникам

[app.py](deploy/app.py) , [stolen_dump.sql](public/stolen_dump.sql) из директории [public/](public/) и IP:PORT сервера

## Описание
-

## Решение

По простоте и константности хэшей нужно понять что это не обычный хэш python. Почитать исходники и найти заметку про маленькие строки, где небезопасный хэш DJBX33A, понять как он работает и написать к нему обратную функцию которая найдёт список подходящих строк. Ввести из каждого списка по одной строке в правильном порядке (из дампа базы)

[solve.py](solve/solve.py)

## Флаг

`cuctf{h45h_c0ll1s10n_1s_m4g1c_c751a9}`
